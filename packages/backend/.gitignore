# convex/_generated IS committed (so CI typecheck/tests run without Convex auth).
# Re-run `pnpm --filter @fammycomforts/backend codegen` (or `dev`) and commit it
# whenever the schema or functions change.
.env.local
