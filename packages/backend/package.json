{
  "name": "@fammycomforts/backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy",
    "codegen": "convex codegen",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@auth/core": "^0.41.1",
    "@convex-dev/auth": "^0.0.94",
    "convex": "^1.40.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@fammycomforts/config": "workspace:*",
    "convex-test": "^0.0.53",
    "typescript": "^5",
    "vitest": "^3.0.0"
  }
}
