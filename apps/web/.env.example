# Convex deployment URL the web client connects to (Story 2.1).
# Copy to .env.local and set to your deployment (matches packages/backend/.env.local CONVEX_URL).
NEXT_PUBLIC_CONVEX_URL=https://YOUR-DEPLOYMENT.convex.cloud
