NEXT_PUBLIC_CONVEX_URL=https://YOUR-DEPLOYMENT.convex.cloud
# BytePlane/ByteAuth launcher URL — sign-in + recovery target (Story 2.2). Cross-repo (spec §5).
NEXT_PUBLIC_BYTEPLANE_URL=https://YOUR-BYTEPLANE.example
