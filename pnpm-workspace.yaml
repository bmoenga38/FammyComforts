packages:
  - "apps/*"
  - "packages/*"

# Native/postinstall build scripts intentionally not run (approve later if needed).
ignoredBuiltDependencies:
  - "@nestjs/core"
  - "@swc/core"
  - esbuild
  - sharp
  - unrs-resolver
